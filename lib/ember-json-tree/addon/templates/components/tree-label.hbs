{{#if this.labelComponent}}
  {{component
    this.labelComponent
    model=this.model
    nodePath=this.nodePath
    selected=this.selected
    highlight=this.highlight
  }}
{{else}}
  <span class='tree-label-text'>{{this.label}}</span>
{{/if}}