{{#if labelComponent}}
  {{component labelComponent model=this.model
    nodePath=nodePath
    selected=selected
    highlight=highlight
  }}
{{else}}
  <span class="tree-label-text">{{label}}</span>
{{/if}}
