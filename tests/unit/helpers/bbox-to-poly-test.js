
import { bboxToPoly } from 'mdeditor/helpers/bbox-to-poly';
import { module, test } from 'qunit';

module('Unit | Helper | bbox to poly');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bboxToPoly([42]);
  assert.ok(result);
});

