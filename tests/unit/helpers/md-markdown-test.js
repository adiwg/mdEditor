
import { mdMarkdown } from 'mdeditor/helpers/md-markdown';
import { module, test } from 'qunit';

module('Unit | Helper | md markdown');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mdMarkdown([42]);
  assert.ok(result);
});

