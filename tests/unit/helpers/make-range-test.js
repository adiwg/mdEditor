
import { makeRange } from 'mdeditor/helpers/make-range';
import { module, test } from 'qunit';

module('Unit | Helper | make range');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeRange([42]);
  assert.ok(result);
});

