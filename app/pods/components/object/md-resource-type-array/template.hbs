<Object::MdArrayTable @columns="Type, Name" @title="Resource Type" @data-spy={{false}} @value={{value}} @validation={{validation}} @plain={{plain}} @required={{required}} @errorMessage={{errorMessage}} @tipPath={{tipPath}} @tipModel={{tipModel}} @profilePath={{profilePath}} @templateClass={{templateClass}} as |type|>
  <td>
    {{#with type.item as |item|}}
    <Input::MdCodelist @create={{true}} @tooltip={{true}} @icon={{true}} @disabled={{disabled}} @allowClear={{true}} @mdCodeName="scope" @showValidations={{true}} @profilePath={{concat profilePath ".resourceType"}} @model={{item}} @path="type" @placeholder="Choose type of resource" />
      {{/with}}
  </td>
  <td>
    <Input::MdInput @value={{type.item.name}} @profilePath={{concat profilePath ".name"}} @placeholder="Name of resource" />
  </td>
</Object::MdArrayTable>
