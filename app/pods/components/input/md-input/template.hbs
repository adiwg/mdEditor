{{#if label}}
    <div class="form-group">
            <label>{{label}}</label>
        {{input
        value=value
        placeholder=placeholder
        required=required
        type=type
        maxlength=maxlength
        class=class}}
        {{yield}}
    </div>
{{else}}
    {{input
    value=value
    placeholder=placeholder
    required=required
    type=type
    maxlength=maxlength
    class=class}}
    {{yield}}
{{/if}}
