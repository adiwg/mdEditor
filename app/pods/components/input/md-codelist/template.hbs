{{#if label}}
    <div class="form-group">
        <label>{{label}}</label>
        <select class="form-control md-codelist" {{action 'setValue' on='change'}} disabled={{disabled}}>
            <option></option>
            {{#each codelist as |code index|}}
                <option value={{code.codeName}} selected={{code.selected}}
                        data-tooltip={{code.description}}>{{code.codeName}}</option>
            {{/each}}
            {{yield}}
        </select>
    </div>
{{else}}
    <select class="form-control md-codelist" {{action 'setValue' on='change'}} disabled={{disabled}}>
        <option></option>
        {{#each codelist as |code index|}}
            <option value={{code.codeName}} selected={{code.selected}}
                    data-tooltip={{code.description}}>{{code.codeName}}</option>
        {{/each}}
        {{yield}}
    </select>
{{/if}}
