{{#let this.buttonConfig as |c|}}
  {{#if c}}
    <button
      type='button'
      class='btn btn-sm btn-{{if c.style c.style "primary"}}'
      {{on 'click' (fn c.action @record)}}
    >
      {{fa-icon 'binoculars'}}
      {{c.title}}
    </button>
  {{/if}}
{{/let}}
