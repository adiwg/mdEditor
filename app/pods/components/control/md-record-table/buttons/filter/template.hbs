{{#if this.showButton}}
  <Control::MdButtonConfirm @class="btn btn-danger btn-block" @onConfirm={{action "deleteSelected" this.selectedItems}}>
    <FaIcon @icon="x" /> Delete Selected
  </Control::MdButtonConfirm>
{{/if}}
