<dt class={{this.titleClass}}>{{if this.title this.title '--'}}</dt>
<dd>
  {{#if (has-block)}}
    {{yield}}
  {{else}}
    {{#if this.text}}
      {{this.text}}
    {{else}}
      <em class='text-muted'>{{this.empty}}</em>
    {{/if}}
  {{/if}}
</dd>