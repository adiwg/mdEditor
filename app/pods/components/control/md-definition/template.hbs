<dt class={{titleClass}}>{{if title title "--"}}</dt>
<dd>
  {{#if has-block}}
    {{yield}}
  {{else}}
    {{#if text}}
      {{text}}
    {{else}}
      <em class="text-muted">{{empty}}</em>
    {{/if}}
  {{/if}}
</dd>
