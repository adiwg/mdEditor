{{#if (has-block)}}
  {{yield}}
{{else}}
  {{#if icon}}
    {{fa-icon icon spin=iconSpin}}
  {{/if}}
  {{text}}
{{/if}}

{{#if tooltip}}
  <EmberTooltip @tooltipClass={{concat "ember-tooltip md-tooltip " tipClass}} @text={{tooltip}} @side={{tipSide}} />
{{/if}}
