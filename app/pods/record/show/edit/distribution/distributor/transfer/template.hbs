<h4 class="section-header">
  Editing Transfer Options for
  <span class="text-success">Distributor #{{distributorId}}</span> in
  <span class="text-primary">Distribution #{{distributionId}}</span>
  <Control::MdButtonConfirm @class="btn btn-xs btn-danger" @onConfirm={{route-action "deleteTransfer"}} @tooltip="Delete Transfer Option?" @tipClass="danger" @tipSide="bottom">
    {{fa-icon "times"}} Delete
  </Control::MdButtonConfirm>
  <Control::MdStatus @model={{parentModel}} />
</h4>

<Object::MdTransfer @model={{model}} @profilePath="record.distribution.distributor.transferOption" />

<ToElsewhere @named="md-subbar-buttons" @send={{component "control/md-button"
  text="Back to Distributor"
  icon="arrow-left"
  class="btn-primary btn-lg"
  click=(route-action "backToDistributor")
  }} />

<ToElsewhere @named="md-scroll-spy-record-edit" @send={{component "control/md-scroll-spy"  refresh=refresh scrollInit=scrollTo setScrollTo=(route-action "setScrollTo")}} />
