diff --git a/dist/packages/ember-babel.js b/dist/packages/ember-babel.js
index 32d05a4de8d7fd117ac5276943b2afef43aec385..d67d1379b1bc62a96cbc18fd09529c9b2f900790 100644
--- a/dist/packages/ember-babel.js
+++ b/dist/packages/ember-babel.js
@@ -163,7 +163,7 @@ export function createSuper(Derived) {
 /*
   Does not differ from default implementation.
 */
-function arrayLikeToArray(arr, len) {
+export function arrayLikeToArray(arr, len) {
   if (len == null || len > arr.length) len = arr.length;
 
   let arr2 = new Array(len);
@@ -174,6 +174,24 @@ function arrayLikeToArray(arr, len) {
   return arr2;
 }
 
+/*
+  Standard @babel/runtime helper. Some @glimmer/* packages are compiled
+  expecting this (and arrayLikeToArray, above) to be exported from this
+  module, but ember-source's own bundled copy doesn't export either one -
+  ember-source only uses them internally for createForOfIteratorHelperLoose.
+*/
+export function objectWithoutPropertiesLoose(source, excluded) {
+  if (source == null) return {};
+  let target = {};
+  let sourceKeys = Object.keys(source);
+  for (let i = 0; i < sourceKeys.length; i++) {
+    let key = sourceKeys[i];
+    if (excluded.indexOf(key) >= 0) continue;
+    target[key] = source[key];
+  }
+  return target;
+}
+
 /*
   Does not differ from default implementation.
 */
